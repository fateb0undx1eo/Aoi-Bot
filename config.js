module.exports = {
    // Command prefix
    prefix: 's!',

    // Auto-post default interval (in ms)
    autoPostInterval: 900000, // 15 minutes

    // Minimum allowed interval (10 seconds)
    minAutoPostInterval: 10000,

    // Meme posting channel ID (replace with your channel's ID)
    memeChannelId: '1390627860527448120',

    // List of subreddits to fetch memes from
    subreddits: [
        'dankmemes',
        'memes',
        'funny',
        'me_irl'
    ]
};
